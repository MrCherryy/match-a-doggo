<h1>Dogs#show</h1>
<p>Find me in app/views/dogs/show.html.erb</p>

<ul>
  <%= cl_image_tag @dog.photo.key, height: 300, width: 400, crop: :fill %>
  <li>Name: <%= @dog.name %></li>
  <li>Breed: <%= @dog.breed %></li>
  <li>Date of birth: <%= @dog.birthdate %></li>
  <li>Age: <%= @dog.age[:year] %> years and <%= @dog.age[:month] %> months</li>
  <li>Gender: <%= @dog.gender %></li>
  <li>Pedigree: <%= @dog.pedigree %></li>
  <li>Address: <%= @dog.address %></li>
</ul>

<%= link_to "Match",  matches_path(authenticity_token: form_authenticity_token, match: { matching_dog_id: current_user.dogs.first, matched_dog_id: @dog.id, status: Match::STATUS[0]}), class: 'btn btn-outline-primary', method: :post%>
<%= link_to "Unmatch",  matches_path(authenticity_token: form_authenticity_token, match: { matching_dog_id: current_user.dogs.first, matched_dog_id: @dog.id, status: Match::STATUS[2]}), class: 'btn btn-outline-primary', method: :post%>

<%= link_to raw('<i class="fas fa-chevron-left"></i>'), dogs_path, class: 'btn btn-outline-primary' %>
<%= link_to raw('<i class="far fa-trash-alt"></i>'), dog_path(@dog), method: :delete, class: 'btn btn-outline-danger btn-sm m-3' %>
