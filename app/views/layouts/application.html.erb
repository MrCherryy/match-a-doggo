<!DOCTYPE html>
<html>
<head>
  <%= favicon_link_tag %>
  <%= favicon_link_tag 'favicon-safari.png', rel: 'apple-touch-icon', type: 'image/png'  %>

  <title><%= yield(:title) %></title>
  <!-- Additional header tags here -->
    <title>MatchADoggo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
    <%= render 'shared/ios_pwa_links' %>
</head>
  </head>

  <body>
    <%= render 'shared/flashes' %>

    <%= yield %>
    <%= render 'shared/navbar' unless current_page?(root_path) || current_page?(new_user_session_path) || current_page?(new_user_registration_path) %>
  </body>
</html>
